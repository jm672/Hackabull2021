var myArray = ['trampoline', 'mall', 'museum', 'concert'];  //Locations in area that fit preferences should be fed into array.
const randomElement = Math.floor(Math.random() * myArray.length); //Select random location.
console.log(myArray[randomElement]); //Print one or more locations to the user.

//im bored and hungry or im just bored.
